using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestPosPlayerToShader : MonoBehaviour
{
    public GameObject bomb;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //Vector4 v = new Vector4(transform.position.x, transform.position.y, transform.position.z, 0);
        //bomb.SetVector("Target", v);
        //bomb.GetComponent<Material>().SetVector("Target", v);
    }
}
